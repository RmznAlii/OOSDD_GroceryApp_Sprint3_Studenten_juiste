<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.GroceryListItemsView"
             x:DataType="vm:GroceryListItemsViewModel"
             x:Name="Page"
             Title="GroceryListItemsView">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Wijzig kleur"
                     Command="{Binding ChangeColorCommand}"
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
        <ToolbarItem Text="Deel boodschappenlijst"
                     Command="{Binding ShareGroceryListCommand}"
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
    </ContentPage.ToolbarItems>
    
    <Shell.TitleView>
        <Grid>
            <Label Text="{Binding GroceryList.Name}"
                   FontSize="Large"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <!-- Hoofdgrid: 2 kolommen (links lijst, rechts producten), 2 rijen (content + message) -->
    <Grid Padding="0,8,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Linker pane: huidige boodschappenlijst -->
        <Border Grid.Row="0"
                Grid.Column="0"
                Stroke="#C49B33"
                StrokeThickness="4"
                Padding="10"
                Margin="10"
                HorizontalOptions="Center">
            <CollectionView ItemsSource="{Binding MyGroceryListItems}"
                            Margin="20"
                            HorizontalOptions="Center"
                            SelectionMode="None"
                            EmptyView="De boodschappenlijst is nog leeg. Voeg eerst boodschappen toe!">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:GroceryListItem">
                        <Grid ColumnSpacing="8" Padding="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4*" />   <!-- 0: productnaam -->
                                <ColumnDefinition Width="*"   />  <!-- 1: aantal -->
                                <ColumnDefinition Width="24"  />  <!-- 2: plus -->
                                <ColumnDefinition Width="24"  />  <!-- 3: min -->
                                <ColumnDefinition Width="Auto"/>  <!-- 4: verwijderen-knop -->
                            </Grid.ColumnDefinitions>

                            <Label Grid.Column="0"
                                   Text="{Binding Product.Name}"
                                   VerticalOptions="Center" />

                            <Label Grid.Column="1"
                                   Text="{Binding Amount}"
                                   VerticalOptions="Center"
                                   HorizontalOptions="End"
                                   Margin="0,0,10,0" />

                            <!-- (optioneel) plus/min iconen -->
                            <Grid Grid.Column="2"
                                  HorizontalOptions="Center"
                                  VerticalOptions="Center">
                                <Image Source="plus.png" WidthRequest="16" HeightRequest="16" />
                            </Grid>

                            <Grid Grid.Column="3"
                                  HorizontalOptions="Center"
                                  VerticalOptions="Center">
                                <Image Source="min.png" WidthRequest="16" HeightRequest="16" />
                            </Grid>

                            <!-- Verwijderen-knop -->
                            <Button Grid.Column="4"
                                    Text="Verwijderen"
                                    BackgroundColor="#FDECEC"
                                    TextColor="#B00020"
                                    CornerRadius="8"
                                    Padding="10,5"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    BorderColor="#B00020"
                                    BorderWidth="1"
                                    Command="{Binding Source={x:Reference Page}, Path=BindingContext.RemoveProductCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>

        <!-- Rechter pane: beschikbare producten + search -->
        <Border Grid.Row="0"
                Grid.Column="1"
                Stroke="#C49B33"
                StrokeThickness="4"
                Padding="8"
                Margin="8"
                HorizontalOptions="Center">
            <VerticalStackLayout Spacing="8">
                <SearchBar
                    Placeholder="Zoek product..."
                    Text="{Binding SearchText}"
                    SearchCommand="{Binding SearchProductsCommand}"
                    SearchCommandParameter="{Binding Text, Source={RelativeSource Self}}"
                    BackgroundColor="#F9FAFB"
                    TextColor="#111827"
                    PlaceholderColor="#9CA3AF"
                    FontSize="14"
                    HeightRequest="44"
                    Margin="0,0,0,6"
                    CancelButtonColor="#6B7280" />


                <CollectionView ItemsSource="{Binding AvailableProducts}"
                                Margin="7"
                                WidthRequest="350"
                                HorizontalOptions="Start"
                                SelectionMode="Single"
                                SelectionChangedCommand="{Binding AddProductCommand}"
                                SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:Product">
                            <Grid ColumnSpacing="8" Padding="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="*"  />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0"
                                       Text="{Binding Name}"
                                       VerticalOptions="Center" />

                                <Label Grid.Column="1"
                                       Text="{Binding Stock}"
                                       VerticalOptions="Center"
                                       HorizontalOptions="End"
                                       Margin="0,0,10,0" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <ContentView>
                            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="8">
                                <Label Text="Er zijn geen producten meer om toe te voegen"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center"/>
                            </VerticalStackLayout>
                        </ContentView>
                    </CollectionView.EmptyView>
                </CollectionView>
            </VerticalStackLayout>
        </Border>

        <!-- Message/feedback onderaan -->
        <Label Grid.Row="1"
               Grid.ColumnSpan="2"
               HorizontalOptions="Center"
               x:Name="Message"
               Text="{Binding MyMessage}"
               TextColor="Red"
               Margin="0,4,0,10" />
    </Grid>
</ContentPage>